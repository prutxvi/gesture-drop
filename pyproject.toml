[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gesture-drop"
version = "1.0.0"
description = "Gesture-based clipboard transfer"
requires-python = ">=3.9"
dependencies = ["flask", "opencv-python", "mediapipe"]
